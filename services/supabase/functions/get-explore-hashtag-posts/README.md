# `get-explore-hashtag-posts`

Returns a cursor-paginated Explore post collection for one normalized hashtag.
The endpoint applies the same post visibility rules as the Explore feed and
hydrates each post with its hashtags for reuse by iOS Explore cards and detail.
