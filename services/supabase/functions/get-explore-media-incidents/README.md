# Get Explore media incidents

Authenticated owner-only recovery queue for active published Explore posts with
direct-R2-confirmed missing media. The caller identity is derived from the JWT;
clients cannot select another owner.
