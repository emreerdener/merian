# Block User

Handles social safety within the Merian discovery feed. When a user blocks another profile, this standard HTTP Edge endpoint updates relations so that the blocked user's public scans are permanently filtered out from the requesting user's feed, and prevents future interactions.
