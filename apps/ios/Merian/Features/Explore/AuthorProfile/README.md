# Explore Author Profile

The `AuthorProfile` directory contains the logic and UI for viewing a public Explore author's profile.

## Purpose
This area manages the public-facing identity of users who share scans to the Explore feed. It exposes privacy-scoped public statistics (like their species count) and non-opening public achievements, allowing the community to learn about the people behind the observations.
