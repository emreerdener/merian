# Explore Shared

The `Shared` directory contains reusable UI components and view models utilized across the Explore feature.

## Purpose
Following the Merian architecture, this directory holds common elements like standard Explore post cards, author avatar views, and shared data models that are needed by the `Feed`, `Map`, or `AuthorProfile`, preventing duplication across the different Explore sub-areas.
