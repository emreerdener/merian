# Explore Map

The `Map` directory contains the geospatial view of the Explore community.

## Purpose
This area presents a `MapKit` visualization of public observations. It relies on the server-side geoprivacy rules (which obscure or hide coordinates based on user settings and endangered species status) to safely render community captures on an interactive map.
