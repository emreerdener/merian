# Insight Chat

The `Chat` directory contains the logic and UI for the Pro-tier Field Chat feature.

## Purpose
This area allows Pro users to ask contextual follow-up questions about a completed biological scan without needing to re-upload raw images. It leverages a dedicated Gemini 2.5 Flash pipeline grounded solely in the saved scan context and generates smart prompt chips to guide the conversation.
