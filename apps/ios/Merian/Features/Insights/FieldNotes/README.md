# Insight Field Notes

The `FieldNotes` directory manages the user's private textual annotations on a specific scan.

## Purpose
This area provides the UI and local persistence logic for users to add and edit personal observations (field notes) attached to their captures. These notes are saved to `LocalScanRecord` and can optionally be published if the scan is shared to Explore.
