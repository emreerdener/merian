# Capture Record

The `Record` directory manages the acoustic capture pipeline on the iOS client.

## Purpose
This area is responsible for "Audio Listen Mode." It records up to 15-second WAV clips and provides users with real-time visual feedback via a live spectrogram and SNR (Signal-to-Noise Ratio) indicators. It allows users to capture bird calls, insect noises, or amphibian sounds for identification.
