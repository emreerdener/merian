# Core Utilities

The `Utilities` directory provides shared helper functions and standard library extensions.

## Purpose
This area contains general-purpose utilities that are used throughout the codebase. This includes date formatters, logging infrastructure, custom array/string extensions, and other lightweight helper logic that doesn't fit into a specific domain layer.
