# Feature Modules and UI Architecture

Merian's UI architecture strictly adheres to a modular, glassmorphic design philosophy, ensuring maximum reuse of Swift components while safely decoupling heavy data operations from the main rendering loop to maintain a continuous 60fps framerate.

## 1. Onboarding & Permission Priming (`OnboardingView`)

To satisfy Apple's strict privacy guidelines and secure high opt-in rates without creating friction, Merian entirely intercepts the cold boot via a programmatic **Permission Priming** sequence. The `Onboarding` feature vigorously adheres to the Domain-Driven folder architecture. The structural state map (`Models/OnboardingStep`) and iOS hardware delegates (`Utilities/LocationPermissionDelegate`) are rigorously decoupled natively from the main UI orchestrator (`Views/OnboardingView`). Each fallback step is natively instantiated inside completely discrete SwiftUI bounds within `Components/` to prevent monolithic view bloat.

- **Enum-Backed State Machine (`OnboardingStep`)**: Radically enforces sequential permission gating (`.welcome`, `.camera`, `.location`, `.photoLibrary`, `.ready`) via a strict `switch` statement overlaying a `ZStack`. This completely prevents users from accidentally bypassing the `AVCaptureDevice` execution prompt by simply swiping (a core flaw of standard `.tabViewStyle(.page)` paradigms).
- **Asymmetric Glassmorphism Integrations**: View state transitions are globally synchronized strictly using `withAnimation(.spring(...))` pushing fluid `.asymmetric(insertion: .move, removal: .move)` structures.
- **DRY Layout Abstraction (`BaseOnboardingStepView`)**: To eliminate SwiftUI boilerplate, all individual steps (`WelcomeStepView`, `CameraPermissionStepView`, `LocationPermissionStepView`, etc.) explicitly inherit their structural geometric bounds and native encapsulated `Rive` shapes exclusively from a generic, highly reusable `BaseOnboardingStepView`. This rigidly protects UI consistency while supporting trailing optional button matrices (such as "Skip for now").
- **Hardware Fallbacks**: 
  - `CameraPermissionStepView`: Asynchronously wraps `AVCaptureDevice.requestAccess(for: .video)` closures directly into the UI state, allowing the system to naturally slide onto the Location view exactly when the iOS permission boundary resolves.
  - `LocationPermissionStepView`: Hooks heavily into a clean `CLLocationManagerDelegate` observing dynamic authorization loops. Crucially, it provides a transparent "Skip for now" fallback button explicitly preserving Apple's App Store Review requirements without alienating free users.
- **Root View Handoff (`MerianApp`)**: Driven explicitly by an `@AppStorage("hasCompletedOnboarding")` physical boundary. The moment the user completes Step 4, SwiftUI dynamically rewires the physical WindowGroup, unmounting `OnboardingView` forever and instantly mapping heavy hardware layers back to the `CameraRootView`. Because `CameraRootView` remains entirely uninitialized in RAM until this flag hits `true`, intense background dependencies (like `AVCaptureSession.beginConfiguration`) are elegantly shielded from the onboarding frames natively.

## 2. The Scans Library (`ScansSearchView`, `ScansSearchManager`)
The Scans tab acts as the user's primary offline biological journal. 

### Native Paging Navigation
- **Horizontal Swipe-to-Navigate**: The primary `ScansSearchView` bypasses strict UIKit `UIPageViewController` limits by entirely utilizing a modern iOS 17 pure SwiftUI `ScrollView(.horizontal)` combined with `.scrollTargetBehavior(.paging)`. This strictly binds to `.scrollPosition(id: $activeTab)` enabling a buttery smooth 1:1 interactive swipe between the "Scans" (Library) and "Collections" arrays while explicitly dropping `.containerRelativeFrame(.horizontal)` wrappers without inheriting native `TabView` safe area anomalies natively.
- **Automatic Search Routing**: The UI features a contextually decoupled search. The `.searchable` prompt dynamically maps to the active `.scrollPosition`, visually promoting "Search keywords, habitats, colors..." or "Search collections..." respectively.
- **Swipe-to-Clear UX**: If the user swipes from one tab to another with an active search filter, the architecture natively intercepts the activeTab `.onChange` to clear the filter and drop keyboard focus, strictly preserving domain boundaries natively.
- **Persistent Bottom Context**: The bottom-anchored, liquid-glass search bar floats elegantly above the `ScrollView` content via native `.searchable`. By stripping background limits natively (`.toolbarBackground(.hidden, for: .bottomBar)` when selection mode is inactive), the interface ensures photo grids flow continuously directly beneath the floating bar.
- **Dynamic Multi-Selection & Grid Scaling**: The `ScansSearchView` implements an explicit trailing `.ellipsis` context menu (abstracted into `ScansSearchToolbarModifier`) strictly when the `library` tab is positioned. The grid options (1x1, 2x2, 3x3) are structured as horizontal quick actions natively utilizing a `ControlGroup` of exclusive `Toggle` elements. This cleanly indicates the selected state and dynamically writes to `@AppStorage("gridColumns")`, instantly reflowing the underlying `ScanGridMatrix` focal geometries seamlessly. It additionally hosts standard "Select multiple" entry boundaries transitioning the entire UI into `isSelectionMode = true`.
- **Contextual Batch Toolbars**: Pushing into `isSelectionMode` instantly replaces the global toolbar. The header flips into a precise tally (`"X Selected"`) bounded by "Cancel" and "Select All" actions natively. The stripped bottom bar dynamically unhides, mounting a labeled `BottomBar` stack strictly containing `Share`, `Download`, and `Delete` actions cleanly interacting across the user's explicit bounds securely. To safely intercept JetSam limits and block `UIActivityViewController` RAM saturation, "Select All" array mappings structurally cap at exactly 20 items smoothly alerting users natively when reaching capacity natively natively!
- **Download Overlays & Notifications**: Tapping the explicit "Download" action dynamically freezes the scroll bounds underneath a synchronous `ProgressView("Downloading...")` block. Because Swift `Task` executions asynchronously batch `.append` calls recursively from Apple’s `PHPhotoLibrary` daemons natively out of memory bounds, preserving selection state physically prevents accidental rapid re-queues. The instant the loop mathematically tracks its completion count natively back onto the `@MainActor` thread, it organically unmounts the loader, drops your explicit selections seamlessly, pulses a success Haptic, and cleanly routes an `.ultraThinMaterial` capsule notification (e.g. *"Saved X photos to your Camera Roll"*) precisely mirroring standard physical Apple feedback patterns natively natively.

### Search & Filtering
- **Native iOS 18 Bottom Search Bar**: Integrates the native iOS 18 `.searchable` and `.searchDictationBehavior(.inline)` modifiers ripped out of the top drawer and dynamically pinned to the bottom placement via `DefaultToolbarItem(kind: .search, placement: .bottomBar)`.
- **Dictation & Clear Mechanics**: Relies strictly on native SwiftUI dictated microphone capabilities and standard 'X' overlays, requiring zero custom `UIViewRepresentable` bindings. Applies `.ultraThinMaterial` toolbars natively matching the aesthetic.
- Driven by `ScansSearchManager`, which utilizes a debounce boundary (`.onChange`) to filter arrays asynchronously without lagging the visual input.
- **Dynamic Filter Bar Eradication & Live Headings**: The UI actively tracks the `isSearchFocused` state via the `.searchable(isPresented:)` binding alongside the `searchManager.searchQuery`. The horizontal sequence binding the filter buttons has been completely abstracted into the highly reusable `CategoryFilterBar.swift` component out of `ScansSearchView.swift`. The component strictly operates on pure Swift `.Binding` layers and explicitly passed string arrays gracefully wrapping native routing closures cleanly out of the monolithic CoreData `ScansSearchManager` constraints organically. The moment a user taps the native iOS search field to bring up the keyboard, the horizontal Category filter pill bar dynamically unmounts. It is instantly replaced natively by a dynamic **"Search library" / "Search results"** contextual header and a live array count tracking the active search, cleanly transitioning state without jarring layout jumps.
- Binds directly to SwiftData's `allRecords` using `@Query(sort: \LocalScanRecord.timestamp, order: .reverse)`. The query explicitly uses a `#Predicate` to filter out non-biological scans (`$0.isBiological == true && $0.commonName != "Unknown Subject"`), keeping the primary library strictly focused on validated nature and completely blocking undetermined edge cases from polluting the feed.
- **Dynamic Biological Taxonomy Pipeline**: Dynamically routes captures explicitly lacking generic class taxonomy (actinopterygii, arachnida, mollusks, rescued unidentified specimens) securely into an explicit "Other" fallback `CategoryFilterBar` pillar natively without vanishing them from the library matrices.
- **Offline Semantic Routing**: Queries filter both explicit user-facing `commonName`/`scientificName` text and invisible `semanticTags` locally embedded by the AI model entirely off-grid (such as 1-3 visually dominant `colors`).
- **LazyVGrid Rendering Resilience**: To prevent fatal SwiftUI rendering engine drops when rapidly swapping multi-thousand item text collections, `ScansSearchManager` strictly enforces explicit `.withAnimation { self.filteredScans = ... }` boundary updates cleanly forcing OS layout calculations natively offline.
- **DRY Scan Matrix (`ScanGridMatrix`)**: To eliminate duplicated layout constants spanning the `ScansSearchView`, `NonBiologicalScansView`, `CollectionDetailView`, and `ScanSelectionSheetView`, the physical 3-column `LazyVGrid` and explicit `Button` action wrappers were fundamentally consolidated into a generic, highly reusable `ScanGridMatrix` component in `Core/UI/Components/`. It securely parses an array of `LocalScanRecord` structures and explicitly relies on an `onDelete` closure and `@ViewBuilder` for seamless context-specific UX boundaries (such as injecting `Remove from collection` action buttons securely) completely agnostic of the underlying global grid.
- **Unified Empty States (`EmptyStateView`)**: Monolithic `VStack` geometries used for 0-result states natively inside `ScansSearchView` and `ScanSelectionSheetView` were entirely purged. They now rigorously inherit the global `EmptyStateView` component mapping to strongly typed dynamic message fallbacks cleanly unifying the missing-data UX natively across the whole library.

### Collections (Top-Level Photo Albums)
- In `MerianSchemaV6`, users can organize `LocalScanRecord` entries into distinct `ScanCollection` buckets.
- **Inline Search Filtering**: The view natively supports inline string filtering against both custom collections and system folders (Favorites, Non-biological).
- Leverages SwiftData `@Relationship` mapping dynamically natively inside a nested 3-column `LazyVGrid`, providing an "Explore Library" modal inside `CollectionDetailView` natively to link IDs safely without duplicating exact 12MP local images.
- Instantly navigates scans from the user's library into explicit folders dynamically, protecting original creation timestamps safely.
- **SaveToCollectionSheetView Eradication**: The standalone `SaveToCollectionSheetView` has been permanently deleted. Collection routing from the `InsightSheetView` is now handled dynamically via a native iOS Menu without pushing opaque sheets.
- **Favorites & Non-Bio Isolation**: A default "Favorites" `ScanCollection` is dynamically seeded for every user. Instead of polluting the `.onAppear` blocks synchronously tying up view rendering matrices natively in both `ScansSearchView` and `InsightSheetView`, this bootstrap initialization logic is now fully centralized. It explicitly drops straight into `ScanRepository.shared.configure(with:)` so the database flawlessly seeds the default folder strictly exactly once at startup. Instead of polluting the primary `LazyVGrid` matrix identically to user-created collections, "Favorites" and "Non-biological" are strictly mathematically filtered out! They permanently bypass the generic `LazyVGrid` entirely, physically elevating into distinct, fixed `DefaultCollectionLink` generic components explicitly pinned to the absolute top of the viewport securely above the user's custom collections matrix, establishing lightning-fast dedicated access instantly upon scrolling. To universally maintain exact mathematical proportions globally across the page, the entire `CollectionsView` layout natively eradicates disjointed explicit `.padding()` closures and is encapsulated strictly within an overarching `VStack(spacing: 16)` engine.
- **Collection State Toasts**: Natively binds a localized `.task(id:)` auto-dismissing `toastOverlay` modifier securely anchored to the `InsightSheetView` hierarchy. This explicitly relays haptic synchronization and glassmorphic `.ultraThinMaterial` pill notifications providing physical confirmation when scans are successfully pushed to or popped from specific `ScanCollection` relations.
- **Collection Management & OOM-Safety (`Rename` & `Delete`)**: The `CollectionsView` deeply integrates explicit `.contextMenu` bounds directly into the active custom `ScanCollection` grid matrix seamlessly allowing users to securely rename or permanently delete personal albums without navigating hierarchies globally. In `CollectionDetailView`, identically routed `Rename` and `Delete` action modifiers seamlessly anchor to a trailing native iOS `Menu` bound securely behind a pristine glassmorphic `.ellipsis` glyph explicitly stripped of legacy frosted `.background` circles. Crucially, executing `Delete` flawlessly bypasses iterative child payload memory loading natively. To natively eradicate redundant inline `.alert("...")` boilerplate throughout the `Views` hierarchy (specifically inside `CollectionDetailView`, `CollectionsView`, `InsightSheetView`, and `ScansSheetView`), the complex destructive database constraints mapping the `SwiftData` relational matrix natively into intrinsic implicit `.nullify` cascaded delete loops were fundamentally extracted out. They explicitly survive completely cleanly mapped inside strictly standalone robust `CollectionRenameAlert`, `CollectionDeleteAlert`, and `NewCollectionAlert` native View Modifiers! To permanently secure offline synchronization parity natively, these changes dynamically initiate a background `syncCollections()` pipeline inside `OfflineQueueManager` intelligently throwing explicit `SyncCollectionPayload` matrices (mapping array `scan_ids`, `id`, `name`, `created_at`) completely cleanly against the upstream remote `sync-collections` Supabase Edge Function downstream!
- **Non-Biological Scans Isolation**: A dedicated list-item navigation button dynamically routes users to a mirror component `NonBiologicalScansView.swift`, which inversely queries exclusively for `$0.isBiological == false || $0.commonName == "Unknown Subject"`. This provides a physical vault for random objects and failed taxonomy inferences without polluting the biological library. To prevent JetSam OOM bounds when extracting physical sizes without rendering grids, the global bounding view `ScansCollectionsGridView` inherently drops the `@Query` mechanism entirely. Instead, it securely maps an asynchronous `Task` calling `modelContext.fetchCount(descriptor)` which computes exact mathematical volume counts cleanly within the SQLite layer. These scalars are statically rendered onto the vault tile without inflating massive memory dictionaries natively! Architecturally, this navigation link physically sits at the bottom of the `Collections` tab viewport, intentionally utilizing tightened `.padding(.vertical, 8)` bounds to bridge the vertical gap seamlessly to standard user collections without abandoning focus.
- **Rescuing & Evicting Taxonomy**: `NonBiologicalScansView` incorporates explicit `.contextMenu` bounds and direct photo-taps triggering iOS Alerts enabling users to rescue edge-case scans dynamically mapping them back to the Biological feed! Since SwiftData `LocalScanRecord` closures regularly lose observer tracking natively inside isolated iOS `.alert` bounds, operations manually execute an explicit `modelContext.model(for: scan.persistentModelID)` fetch, flawlessly hooking into the native `@MainActor` thread! Scans instantly re-mutate `.isBiological = true`, and dynamically morph "Unknown Subject" string barriers permanently to "Unidentified Specimen" natively. This organically bypasses both `@Query` arrays instantly, permanently evicting them from the Non-Biological vault and inserting them beautifully back into the central Scans library visually. Additionally, a pure "Clear All" destruct command allows executing a physical batch eradication loop completely mapping over lightweight `BackgroundDatabaseActor.ScanErasurePayload` properties cleanly eliminating massive physical gigabyte files silently through isolated `Task.detached` blocks instantly!
- To maintain radical transparency regarding storage constraints, the `NonBiologicalScansView` seamlessly injects a dynamic `.blue.opacity(0.1)` 30-day auto-purge informational banner rendering smoothly above the core layout grid to reassure users natively of background system cleanses securely.

### Main Tab Bar
- **Bottom Position Alignment**: The `Explore`, `Scans`, and `Profile` primary actions are strictly hoisted to a custom floating liquid glass capsule component (`MainTabBar`) overlaid at the bottom of the camera feed. It intelligently maps inactive features directly with distinct `"Coming soon"` `Color.blue` status pillows explicitly guiding users securely.
- **Ultra-Thin Material**: To maintain an immersive glassmorphism experience, the tab bar leverages iOS's native `.ultraThinMaterial` background. This automatically adapts to the visual contrast of whatever the camera is currently looking at natively responding to the environment, paired with a specular border (`.strokeBorder`) gradient and soft drop shadow.
- **Sheet Navigation**: Tapping any navigation element natively spawns `.sheet` structures (`SettingsView`, `ProfileView`, etc.) that slide completely over the camera interface to strictly prevent any structural re-routing of the global hierarchy or interruptions to the `.isCapturing` hardware constraints.

### Memory Integrity (`ImageDownsampler` & Concurrency)
- Natively guards the iOS lifecycle against "Out of Memory" (OOM) crashes by strictly decoupling explicit `Data` buffer conversions out of SwiftUI into an isolated `ImageDownsampler` static abstraction, leveraging Core Graphics interpolations dropping massive 12MP files seamlessly into RAM sequentially rather than instantiating memory-heavy `UIImage(contentsOfFile:)` chunks across grid loads.
- **Thread Starvation Prevention**: Execution arrays natively hitting Apple's `ImageIO` functions (`CGImageSourceCreateWithURL`) are explicitly moved cleanly into `Task.detached(priority: .userInitiated)` structures. This seamlessly protects the Swift 6 global cooperative thread pool from halting the main UI rendering matrix sequentially.
- **Historical Payload Override**: To entirely avoid destroying device cellular data limits downloading massive multi-gigabyte historical image files physically into iOS, the hydration engine (`ScanRepository.syncHistoricalScansDown`) smartly routes the specific historical Cloudflare R2 URL dynamically directly into the SwiftData payload `.localImagePath` and `.additionalImagePaths` while keeping `.referenceImageUrl` strictly bound to Wikipedia/GBIF. This explicitly routes natively bypassing image duplication loops! Specifically, both `ScansThumbnailView` and `AsyncLocalImageView` pipe these paths seamlessly into the thread-safe `LocalImageLoader` actor. If missing from Sandboxed Documents locally, the actor robustly executes a `fetchNetworkFallback` mapping remote payload bounds via `URLSession` organically shielding all structural frontend view code from conditional 50-line `if let` blocks completely.
- **Sandbox Resiliency**: Physical disk file references completely drop absolute path trails prior to parsing (via `.lastPathComponent`). This gracefully secures the `LocalImageLoader` payload extracting `Documents/` payload bytes dynamically without rendering broken thumbnails when the OS natively shifts active container mount paths (e.g. during Xcode Simulator rebuilds or iOS firmware updates).

## 3. Inferences & Telemetry (`InsightSheetView`)
The `InsightSheetView` is Merian's central contextual readout, triggered immediately after an Edge API loop or seamlessly opened offline via the Scans library.

### Core Rendering Logic
- **Modular Component Isolation**: The `Insights` feature explicitly defines a localized Domain-Driven folder hierarchy to guarantee maximal compilation times and separation of concerns. Top-level fullscreen layouts (`InsightSheetView`, `DiagnosticComparisonView`) are strictly hoisted into `/Views`. 
  - The massive structural `SwiftUI` `@ViewBuilder` layout blocks previously hardcoded in `InsightLayout.swift` were completely decoupled. The core `InsightHeader` strictly recouples the textual `Description` loop entirely into the localized semantic `VStack` tracking the Scientific and Common Names natively. This guarantees perfect sequential typographic alignment, while physically isolating the dynamic `SpeciesBadges` grids cleanly down into the outer container boundaries untouched. The Biological view explicitly maps pure typography layers dynamically eradicating primitive structural left-icons strictly shifting explicit glyph matrices inline matching right-aligned layouts. This inherently injects exact local `timestamp: Date?` and physical vertical `gpsElevation` cascade bindings natively resolving absolute chronological and altitudinal mapping geometries inside the Environment Card seamlessly!
  - `DiagnosticComparisonView` specifically renders the AI's flattened array string bullet points dynamically parsing raw trait differentials strictly into scalable `HStack` layouts instead of legacy HTML-style data grids.
  - Structural sub-elements (`ImagesCarousel`, `ToxicityBanner`, `Badge`) are decoupled identically into `/Components`. Underlying SDK wrappers (like the UIKit `SFSafariViewController` bridge) are relocated out of the Insights domain entirely into `Core/UI/Components/SafariView.swift` for universally clean reuse. Moreover, isolated modules like `ReportInsightView` are heavily strictly refactored pushing raw API `Task` network closures natively out into `ReportInsightViewModel.swift` obeying pure `@MainActor` MVVM! This totally eliminates monolithic UI bundles.
- **Adaptive Dark Mode & System Materials**: All hardcoded dark-mode overrides previously limiting the modal have been explicitly eradicated. The sheet maps entirely to Apple's adaptive `.ultraThinMaterial` backgrounds responding natively to the user's system appearance correctly maintaining accessibility contrast bounds dynamically. The isolated `Badge` structures fundamentally extract heavy aesthetic logic enforcing `isFilled` attributes chemically reacting natively by lowering `.opacity(0.6)` on flat `.color` layers creating pristine, Apple-tier Liquid Glass capsules exclusively bound natively by `in: Capsule()` overlays. Furthermore, `Badge` structures are meticulously hardened utilizing `.fixedSize(horizontal: true, vertical: false)` natively locking the boundary exclusively to its internal content scale, neutralizing aggressive `maxWidth: .infinity` stretching from greedy parent `.frame` arrays! They dynamically implement explicit generic optional `icon` parsing, smoothly degrading into purely sleek typographic string pills encased organically inside `.ultraThinMaterial` bounds natively. To push Apple Intelligence tier aesthetics, the `ConfidenceBadge` specifically parses mathematical floats into explicitly formatted Hybrid Semantic Strings (`95% - 100% • High confidence`), dynamically generating strictly mapped Emerald and Red `.sparkles.2` iconography. It overlays these primitives universally across all confidence tiers with a breathtaking Holographic Foil Sweep tracking an explicit `.linear(duration: 4.5)` offset! Furthermore, tapping the badge cleanly triggers a natively decoupled `ConfidenceExplanationSheet` leveraging `.presentationDragIndicator(.visible)`. Inside, it renders a profoundly sophisticated Continuous Spectrum Timeline structurally relying purely on explicitly bounded `Rectangle` scaling to flawlessly draw a visually unbroken connecting gradient pipeline bridging each nested semantic node natively.
- **Safety Critical Block**: `ToxicityBanner` parses `speciesData?.insightData.isPoisonous` instantly displaying yellow cautionary alert ribbons above the fold to guarantee hiker safety natively without triggering alarmist fear. It gracefully reads "This subject could be poisonous" paired with an `exclamationmark.circle`. All legacy fallback informational string boundaries explicitly inside the `else` logic block have been totally stripped out, seamlessly defaulting the banner natively into an `EmptyView()` rendering structure to radically preserve zero-footprint vertical UI geometry bounds if a subject is mathematically deemed non-toxic!
- **Ecological Validations & Legacy Hydration**: Binds fallback indicators for "Not biological" or "Not a live capture" gracefully routing edge failure cases into a clean UI without hard-crashing. If an item scores `<85%` confidence, it triggers the `DiagnosticComparisonView`. Additionally, isolated `Badge` instances explicitly handle legacy historical payloads seamlessly rendering weather/location geometries safely if the dataset pre-dates the MapKit reverse-geocoding era. `ScanInformationCard` dynamically mathematically evaluates `.gpsElevation` data, universally blocking flat `0.0` initialization artifact variables structurally ensuring completely authentic topological reporting visually implicitly.
- **Liquid Glass Edge-to-Edge Parallax**: `InsightSheetView` natively overrides iOS 16 `.sheet` drag-indicator padding bugs explicitly utilizing `.ignoresSafeArea(.all, edges: .top)` directly onto the native `ImagesCarousel` Geometry bounds! Crucially, the sheet successfully integrates a "Stretchy Header" dynamic parallax completely natively inside the isolated `InsightContentView` `ScrollView`. It continuously evaluates a custom `.coordinateSpace(name: "InsightScrollSpace")`, capturing `minY` offset triggers dynamically shifting the background mathematically precisely upwards (e.g., `offset(y: -scrollY)`) completely negating native rubber-band bounce tearing organically! Furthermore, to structurally block phantom multi-axis bleeding layout strokes, its primary `VStack` is aggressively clamped horizontally to absolute screen geometries, permanently discarding erroneous phantom horizontal swipe capabilities natively!
- **Overscroll Bleed Buffer & TabView Resilience**: Because native iOS 17 pure `ScrollView` horizontal paging natively introduces a severe bug inside `.sheet` layouts (where rapid swipes leave elements frozen exactly halfway between boundaries), the `ImagesCarousel` strictly abandons pure SwiftUI grids and relies on the legacy `UIPageViewController`-backed `TabView(.page)`. However, to securely eradicate the notorious 1-frame cross-framework gesture tearing bug (which violently rips open a white gap when pulling the `TabView` vertically), the parallax Geometry bounds dynamically render a rigid "Overscroll Bleed Buffer". It physically generates 50px of hidden underlying image data forced directly upwards out of the bounding viewport. This guarantees mathematical visual immunity against physical frame clipping lag during aggressive bouncy sheet interactions!
- **Dynamic Contextual Header**: To perfectly replace the `ConfidenceBadge` with the truncated Common Name only when strictly required during active scrolling sequences, the entire Layout maps an invisible `GeometryReader` onto a deterministic native `ScrollView` coordinate tracking axis `"InsightScrollBoundary"`. It calculates the absolute text boundaries dropping `.infinity` drops safely, shifting UI `.principal` payloads organically!
- **Inline Action Buttons & Vertical Taxonomy**: Core data modifiers (Add to Collection, Flag for Review) were natively ripped out of the secondary functional menus and promoted directly below the core description block into massive, full-width `.capsule` gradients for rapid ergonomic execution. The Linnean classification natively abandons legacy horizontal scrolling `.TaxonomyNode` pills entirely, transforming structurally into a sleek, vertical key-value `.glassCard` dictionary explicitly outlining the terminal SPECIES text natively.
- **Scans Contextual Imagery**: When opened directly from the historical Scans library, the system intelligently intercepts the UI routing and physically forces the *user's natively captured local photograph* to dominate the Hero Carousel, rather than defaulting to the Wikipedia/GBIF reference imagery. This builds profound personal connection to the data.
- **Decoupled Asynchronous Validations**: To protect rendering bounds cleanly, all asynchronous `FileManager` fetches structurally identifying historic local payloads are cleanly removed from the legacy `InsightCarousel` (now `ImagesCarousel`) and natively isolated into the background threaded `load` pipeline inside `InferenceEngine`.
- **Isolated Animation Engine**: Features a complex, continuously rotating 360-degree `LinearGradient` (rainbow styling) for pending AI states. To guarantee perfect 60fps frame rates without stuttering, the animation logic is rigorously decoupled and strictly bound inside a localized struct/equatable bound, completely isolating it from `CameraViewModel` SwiftUI layout redraw strikes.
- **Visual Virality & Sharing**: Implements a fully native `UIActivityViewController` pipeline explicitly built to stage rich imagery payloads perfectly. When tapped, the system dynamically intercepts the actual photograph (live capture, local disk cache, or safely pulled Cloudflare URL). Crucially, to prevent the main `@MainActor` UI thread from violently stuttering or completely freezing when extracting massive raw 12MP payloads natively via `Data(contentsOf:)` and `UIImage(data:)`, these operations evaluate completely out-of-bounds inside a strict `Task.detached(priority: .userInitiated)`. To brutally eliminate duplicate recursive iOS RootViewController `UIActivityViewController` presentation loops universally, the manual rendering bounds explicitly instantiated synchronously inside `InsightSheetViewModel` and `ScansSheetView` were entirely purged! They flawlessly drop natively directly into a global isolated `ShareSheetUtility.present(items:)` pipeline organically binding iPad Popover structural safety securely! Any local file retrieval dynamically executes an absolute path expansion (`URL.documentsDirectory.appendingPathComponent(path)`) prior to extraction, ensuring iOS Share Sheets actually render the payload map rather than silently dropping abstract local filename strings natively. Additionally, the remote URL parsing actively trims invisible whitespace characters (`.trimmingCharacters(in: .whitespacesAndNewlines)`) previously left behind by legacy array decoders preventing fatal `nil` URL resolutions natively. To prevent the iOS Messages app from aggressively discarding the `UIImage` physical attachment in favor of a "Rich Link Bubble", the `merian.app` Universal Link is explicitly folded directly into the core `String` text message rather than passed as a standalone `URL` object to the iOS Share Sheet natively.
- **Exporting Raw Photography (`Save my photos`)**: Inside the toolbar menu, users can extract their biological captures directly to the iOS Camera Roll universally without triggering the Auto-Save settings. It actively parses `PhotoLibraryManager.saveImageManual(imageData:)` with `.addOnly` permissions natively filtering the `inferenceEngine` remote payload arrays. Like the share sheet, this strictly requires exact `.documentsDirectory` resolution loops across iOS Sandbox security boundaries natively, shielding Wikipedia/GBIF external references from downloading inherently.
- **Optimistic UX (Deletions)**: Users can permanently obliterate local and global data securely via `.contextMenu` bounds (Library, Collections) or the Toolbar `Menu` inside Insight bounds. Instantly bounding a standard native `.alert("Delete scan?")` modal logically superseding constrained Action Sheet bugs deeply nested natively! Pressing "Delete" executes an immediate zero-latency removal from the local UI matrix, triggering a heavy `HapticManager.shared.triggerErrorThump()` drop, while securely routing physical R2 bytes and PostgreSQL rows to the `PendingCloudDeletionTask` detached background queue invisibly to not block the UI thread.
### External API Enrichment
- Spawns parallel external lookups fetching the full taxonomic classification into the visual `TaxonomyCard`.
- To ensure the fastest user experience without blocking the initial insight response, the iOS client autonomously fetches Wikipedia data independently. Immediately after the Gemini payload renders the underlying components, `InferenceEngine.asynchronouslyFetchWikipediaAndHydrate` natively pings the Wikimedia REST API (e.g., `/api/rest_v1/page/summary/`). Because `speciesData` relies on unstructured `@Published` bindings natively, when the data resolves on the Swift actor, the Wikipedia abstract paragraph dynamically fades directly into the active `InsightSheetView` organically without mandating modal dismissal limits or UI locks.

## 4. Account (`ProfileView`)
The primary identity portal bridging local usage limits with the Supabase Ghost Session ecosystem. The legacy monolithic list was completely refactored into a sleek dual-pane generic pagination structure matching the Scans Library! `ProfileView` acts directly as a structural orchestrator: utilizing a central horizontal `.paging` `ScrollView` controlled seamlessly by a `ToolbarItem` segmented picker. It maps precisely between a `ProfileTab.profile` layout (isolating identity, statistics, and hardware capabilities) and a `ProfileTab.settings` layout explicitly housing hardware preferences, data exports, and the Danger Zone. Complex side effects are safely deferred to `ProfileViewModel`, leaving `ProfileView` uniquely purposed for gesture-driven layout abstractions cleanly without stutter!

### Native OAuth & Entitlements
- Implements purely native Swift `SignInWithAppleButton` and `GoogleSignInButton` SDK boundaries securely retrieving external `.idTokens`. These buttons are dynamically unmounted and embedded cleanly at the *very top* of the active View directly inside the `ProfileTabView` (above the Terrarium statistics) if the `supabase.isGuestUser` condition resolves, cleanly erasing nested setup friction globally.
- Reassigns initial Anonymous IDFV GoTrue Sessions natively merging user arrays into persistent Cloud records.
- Instantly pulls explicit `RevenueCatManager.shared.isProActive` booleans conditionally hiding or throwing the `PaywallView` dynamically if they hit their daily 2-scan limit. The Profile natively leverages this observed property migrating the generic "Manage Plan" row entirely into an upscale `PlanCard` dynamically nested inside the header, outlining explicit capabilities (Naturalist UNLIMITED SCANS vs Explorer 2 SCANS DAILY). The `PaywallView` seamlessly inherits physical system color schemes dynamically rendering in stunning Light or Dark mode.
- Renders global gamification statistics including `uniqueSpeciesCount` and `currentStreak` in the stats grid, while evaluating the user's `persona` (Explorer Rank) dynamically purely offline within the profile header. Both the `StatCard` modules and `ProfileTabView` bypass native list limits completely by deploying geometric `24px` Continuous Squircles driven strictly through `.background(...)` shape modifiers explicitly bounded away from intrinsic OS table cell clipping masks. Instead of blocking the UI on remote PostgreSQL network requests, it utilizes heavily optimized `SwiftData` `@Query` property wrappers mapping array statistics natively preventing UI lag and dropping network errors cleanly for a flawless "Digital Terrarium" profile reflection.

### Awards & Milestones UI
- **Visual Hierarchy & Safe Geometry**: Features a premium vertical `VStack` layout natively stacking discrete component files structurally down the primary view bounds. To preserve strict UI abstraction modularity, massive monolithic UI files were shattered: `AwardsAndMilestones.swift` functions solely as a top-level orchestrator cleanly importing fully isolated `Features/Profile/Components/Profile/AwardCard.swift` capsules! Crucially, `ProfileTabView` permanently ripped out the problematic legacy `List` UIKit wrappers (which broke Apple's iOS 17 horizontal `.paging` layout buffers mathematically enforcing double top-safe-area padding limits). It gracefully swapped to a flawless pure `.grouped` background explicit `ScrollView`, natively destroying internal generic hidden headers inherently!
- **Dynamic Sorting Control & Temporal Heuristics**: The master header incorporates a native iOS `Menu` bound to an overarching `@State` enum (`AwardSortOption`), empowering users to seamlessly re-order the layout dynamically via an elegant spring transition. The default `Smart sort` completely abandons explicit completion binary boundaries and specifically executes an advanced mathematical heuristic priority scalar seamlessly evaluating contextual interactive momentum natively computed entirely offline directly inside `ProfileDatabaseActor.calculateAwards`! The hierarchy organically bubbles active achievements visually: **Tiers >= 3.0** distinctly isolate mathematically unlocked (`isCompleted`) goals explicitly triggered natively within exactly 7 days (Hero Status)! **Tiers 2.01–2.99** seamlessly map to natively incomplete but active goals (In-Progress), bubbling mathematically close fractions like `2.95` natively over `2.10`! **Tier 1.0** mathematically maps Legacy Completed achievements older than a week systematically sinking them strictly beneath active bounds organically, while **Tier 0.0** cleanly buries mathematically unstarted goals `(0%)` securely! In the event of explicit identically calculated fractions clashing tier boundaries natively, the engine securely cascades back exactly isolating chronological `lastInteractionDate` timestamps acting as a strictly infallible secondary tie-breaker seamlessly!
- **Premium Specular Shimmer Engine**: To elevate user achievement tangibility, the explicitly bounded 48x48 icon `ZStack` internally hosts an ultra-premium `LinearGradient` generating a continuous diagonal ray gracefully sweeping across an explicit `.overlay`. It fundamentally matches the breathtaking holographic foil offset tracking found natively on the `ConfidenceBadge`! Crucially, the animation strictly discards stuttering infinite `.onAppear` recalculations natively for a completely stochastic, randomly-interpolated asynchronous `Task` organically throwing decoupled 1.8-second flashes silently between 4s-12s delays, inherently preventing 120Hz GPU locking dynamically!
- **Vector-Bound SF Iconography**: Every badge structurally anchors an SF Symbol `Image(systemName:)` strictly using explicit `.resizable().scaledToFit().frame(width: 24)` attributes safely bounded securely instead of typography `.font` layouts inherently structurally eliminating unconstrained axis bleed entirely visually regardless of device Accessibility Dynamic Type values or abnormal symbol scales. They natively calculate a difficulty `Color` engine (Spring Green / Amber / Deep Crimson) organically applied functionally onto intuitive right-aligned difficulty pills.
- **Progress Meter Implementation**: Every badge dynamically tracks progress seamlessly linking intrinsic tracking integers (e.g., `3 / 10`) mapped directly against a fluid SwiftUI `GeometryReader` custom dual-`Capsule` progress bar, fundamentally eradicating standard `ProgressView(value:)` native artifacts definitively! Upon completing the total count (`isCompleted == true`), the layout transitions gracefully into an unlocked state entirely unmounting the Progress capsule gracefully, injecting a customized `Capsule` text overlay (`Completed`), bounding an explicit `.strokeBorder` mathematically mapping the native badge core tint color, and illuminating a localized checkmark! The entirety of the global `AwardCard` geometry perfectly isolates inside `Color(uiColor: .secondarySystemGroupedBackground)` natively granting mathematically correct dark mode boundary separation.

### Aesthetic Customizations
- Contains the `AppIconManager` allowing `Pro` users to natively swap their iOS Springboard Icon using `UIApplication.shared.setAlternateIconName` natively. This natively updates both the app boundary and the underlying `Config.xcconfig` bounds safely without requiring a restart.

### Contribution Heatmap
- **ScansHeatmap**: Natively visualizes user's biological scanning frequency over time via a pristine, GitHub-style 52-week contribution graph natively embedded directly onto the background canvas without heavy UI card wrappers. It seamlessly features a dual-state layout toggle (`.year` vs `.month`), allowing users to organically zoom into recent temporal discovery data with an animated transition.
- **Persistent Scale State**: The layout state aggressively leverages `@AppStorage("heatmapScaleSelection")` rather than ephemeral `@State` abstractions natively. On a user's absolute first app launch, the graph mathematically defaults entirely into the accessible `.month` viewport. If they manipulate the `.year` scale toggle, the string selection locks into `UserDefaults`, preserving their exact visualization preference completely natively upon their next organic cold boot.
- **LazyHStack & Memory Matrix Isolation**: Instead of using a massive iterating `HStack` which forces the entire 364-node array into RAM, the layout explicitly integrates a pure `LazyHStack` structurally wrapped inside a heavily-typed, perfectly isolated `HeatmapGridMatrix: View, Equatable` component map! The SwiftUI renderer cleanly inherently caches the entire struct organically preventing continuous layout passes generated by the global view `.onChange` triggers seamlessly forcing 100% onscreen rendering instantiations mathematically preserving perfectly preserved Y-Axis alignments dynamically securing 120Hz speeds structurally without crashing hardware graphs! 
- **Isolated Data Pipeline**: Render states are explicitly generated off-thread in the `ProfileDatabaseActor` as a flat Sendable array map and bound seamlessly directly into the view array matrix, guaranteeing absolutely no O(N) date-math blockages on the SwiftUI Main Actor thread. Furthermore, the `totalCaptures` scalar metric aggregates uniquely explicitly inside the active 52-week array generation loops natively, organically bypassing arbitrary lifetime `.reduce` counts to guarantee 1:1 mathematical accuracy mirroring the visual 364-day dataset exclusively. The `totalCaptures` metric responsively filters to match the active timeframe (either 365 days or 30 days) when the scale toggle is activated.
- **Dynamic Geometric Width Expansion**: To natively adapt horizontal limits without layout recalculation crashes bounding out-of-screen bounds, legacy explicit pixel arithmetic mapping `GeometryReader` widths was permanently eliminated! For `.month` scale, horizontal bounds radically shift: the component bypasses rigid `LazyHStack` behaviors dynamically wrapping the matrix directly into a pure `HStack` equipped with individual `.frame(maxWidth: .infinity)` constraints. The week-columns automatically divide available screen width evenly without bleeding past the screen edges or blocking viewport margins securely. During `.year` layouts, grid elements gracefully degrade into strict, rigid `1x1 11pt` blocks!
- **Conditional FadingScrollView Geometry Tracking**: To perfectly orchestrate seamless edge-to-edge scrolling natively without clipping or popping UI bounds, the `.year` matrix is enclosed inside an extracted `FadingScrollView`. This utilizes rigorous `GeometryReader` tracking bounds to map dynamic `.clear` gradients to fade the overlapping edges cleanly dropping synchronous UI updates securely into isolated `.task` scopes to defeat continuous re-render crashes natively! Crucially, the `.month` scale explicitly completely bypasses the `FadingScrollView` container, bypassing `.trailing` scroll-alignments and mask trailing bugs to render directly as a static locked grid cleanly filling the active screen bounds perfectly!

### Hardware Preferences
- **Theme Segmentation (Symbol Anchoring)**: Because Apple's native iOS `.pickerStyle(.segmented)` natively limits UI boundaries rendering exclusively Text identifiers smoothly across platforms, Merian respects Apple HIG rigidly natively! It explicitly scraps legacy custom interpolation wrappers (`HStacks` attempting to force arbitrary SF Symbol layouts) uniquely falling back to elegant, pure system `Text("Light")` bindings. This mathematically guarantees crisp `.segmented` boundaries perfectly mapping physical iOS Segment transitions effortlessly!
- **Expedition Mode**: Manually throttles the `HardwareOrchestrator` to 24fps and disables intensive visual blurs to preserve battery.
- **Live Viewfinder Hints**: Allows users to manually disable real-time AI scanning hints before capturing to securely reduce thermal loads or minimize battery drain.
- **System Haptics & Camera Roll**: UserDefaults bindings to dynamically skip `HapticManager` calls or directly prevent `PhotoLibraryManager` from pushing raw buffer bytes into the iOS Photos ecosystem. Tactile interactions directly overlay `.heavy` feedback parameters onto the Camera Shutter explicitly complementing `.medium` responses tied to local hardware triggers like the Flash toggle seamlessly bypassing layout thread blocks.

### Privacy & Science
- **Geoprivacy Control (`SettingsGeoprivacyView`)**: Extracted into a dedicated sub-page providing comprehensive user education on coordinate tracking natively. Contains detailed explanations for `Open` (raw sharing for researchers), `Obscured` (50km radius randomized blurring), and `Private` (completely hidden from discovery feed). Modifying selections elegantly cascades a Supabase edge update to the `users` PostgreSQL table immediately in the background via closures. Completely accessible to Ghost Users mapping natively to their anonymous UUID.
- **Export Scans (DwC-A)**: *(Auth Required)* Connects to the `/export-dwca` Deno task through a `Task.detached` thread to pull a `.zip` archive URL back efficiently natively rendering a `ShareLink` payload safely out of memory constraints. Fallback "Sign in with Apple" prompt prevents anonymous users from generating payloads before creating an account.

### Danger Zone & Data Lifecycle
- **Local Cache Management**: Allows dumping `ImageCache.shared` and orphaned `/Caches/` JPG payloads directly off the iPhone flash memory. This directory enumerator is structurally guarded (`!fileURL.lastPathComponent.contains("_temp_upload")`), actively shielding background `OfflineQueueManager` URLSession transfers natively mid-sync to prevent violent network daemon crashes when users manually clear app storage.
- **Account Erasure**: Features a hardline "Delete Account & Data" action executing a native `.destructive` `.confirmationDialog`. This explicitly routes down through the "Danger Zone" Section logically mapping directly beneath the "Sign Out" authentication boundary protecting explicit application layouts continuously. This forces a sequential 4-part wipe: triggering the Deno `/safe-delete` endpoint natively, destroying the GoTrue identity locally (`signOut`), and explicitly dumping the entire device SQLite boundary dynamically (`ScanRepository.shared.purgeAllData`) pushing the user gracefully back into the generic Camera interface smoothly without crashing.


## 5. Hardware Integrations (`AVCaptureEventInteraction`)
- Directly hooks into iOS 17.2 tactile hardware boundaries ensuring that depressing physical hardware configurations natively triggers the exact same `EnvironmentContextManager` background `MKReverseGeocodingRequest` and `WeatherService` hooks seamlessly as the screen UI.
- All points of access guarantee zero-latency capturing and GPS binding natively mapped straight into EXIF metadata directly inside the user's core iPhone Photo Roll.

## 6. UI Abstract Modularization
- The UI hierarchy strictly avoids massive structural bindings. The primary navigational routing structure completely avoids UIKit toolbars, abstracting natively from the monolithic `CameraRootView` into an isolated `MainTabBar` capsule component floating 24pts off the bottom horizontal bounds. All core camera utilities (`PhotosPicker`, Flash Toggle, Hint Banner, Shutter Button) were further explicitly extracted completely entirely out of `CameraRootView.swift` into a dedicated `CameraControlsOverlayView.swift` component structurally decoupling the UI layers from the `NavigationStack` orchestrator completely. Natively bounding the absolute top Safe Area, it features `MediaModeToggle`—a bespoke `.ultraThinMaterial` glassmorphic selection pill isolating `Scan` vs `Record` sensory modalities mapping organically back into the app's core vernacular. To push purely modern aesthetics natively, `MediaModeToggle` explicitly eradicates all 3D `.shadow` drop-layouts globally, brutally forcing the interactive geometry exclusively into a perfectly flat, pure translucent interface! By intercepting Apple `UIImpactFeedbackGenerator` triggers cleanly with an isolated asynchronous `@MainActor` Task, it floats a beautiful localized "Coming soon" generic popover dynamically beneath unresolved staging endpoints without resorting to global alert modifiers! Unlike legacy iterations, `CameraControlsOverlayView` inherently abandons global `@EnvironmentObject` and `@ObservedObject` managers. The entire architecture explicitly migrated to the Swift 17+ `@Observable` macro seamlessly, natively injecting environments exclusively via `@Environment()` and routing explicit UI updates only for variables actually read by views. It strictly takes pure intent closures (`onCapture: { viewModel.executeCapture() }`) mapping perfectly into dynamic explicit primitive definitions cleanly without triggering re-renders globally.
- `Camera` natively separates layout (`Views/CameraRootView`, `Views/ScanningOverlayView`, `Views/ImageCropperView`), state logic (`ViewModels/CameraViewModel`), and modular sub-views (`Components/ThermalWarningOverlay`, `Components/ViewfinderHintBanner`), completely uncoupling the old monolithic `CameraOverlayComponents` script. Additionally, it structurally shields against "Silent Scan" UI drops: if the user aggressively backgrounds the app during an active AI lookup, `CameraViewModel` explicitly refuses to reset `isAnalyzingFullscreen = false` via an `inferenceEngine.isProcessing` guard, ensuring that upon restoring the app into the foreground, the completed Modal sheet bounds present exactly as expected organically without rendering empty views natively.
- `ProfileView` natively abstracts completely! The interface abandons a massive scrolling list and natively routes paginated domain layouts. The primary `Profile` pane maps directly into `ProfileTabView` (Top-anchored Inline Apple/Google Auth routing, Avatar metadata, Explorer Persona, followed by nested `UserStats` array aggregations safely structured below explicit identity boundaries). The secondary `Settings` tab houses decoupled structural bounds explicitly organized into `ProfilePreferences.swift`, `ExportScans.swift`, `SettingsCommunity.swift`, and `SettingsDangerZone.swift` components! This explicitly isolates structural UI code leaving `ProfileView` strictly as a fluid horizontal `.paging` layout orchestrator.
- The `Scans` feature rigorously enforces Strict Domain-Driven Folders, entirely shattering the monolithic `ScansSearchManager.swift`. The pure array search `.task` logic executes strictly within `ViewModels/ScansSearchManager`, while massive 300+ line SwiftUI declarations (`ScansSearchView`, `ScanSelectionSheetView`, `CollectionDetailView`) inherently live in `Views/`. Foundational models (`Models/SearchableScan`) and discrete Apple UIKit rendering mechanisms natively resolve outside the feature boundary completely mapping directly to global singletons or the overarching `Core/UI/` component directory universally. Check `docs/features-and-hardware/components-guide.md` for explicit global interface primitives (`EmptyStateView`, `ArchivedVisualsView`, `ScanDeletionDialogModifier`).
- **Redundant Theme Binding Cascades**: Previously, massive `.preferredColorScheme(themeMode.colorScheme)` declarations and `@AppStorage` dependency strings were unnecessarily duplicated strictly across child view hierarchies (`CameraRootView`, `CameraSheetRouter`, `ProfileView`). This natively caused disjointed structural view environment tearing during rendering updates. The architecture completely purges these variables out of local bounds, strictly establishing the master configuration securely injected at the absolute root `MerianApp` layer, natively empowering the environment color scheme to cleanly uniformly cascade structurally mapping all underlying `.sheet` presentations optimally without duplicated instantiation.
