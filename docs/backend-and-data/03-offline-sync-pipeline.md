# The Offline Synchronization Pipeline

Merian's core differentiator is treating Off-grid nature encounters as a first-class citizen using native Apple offline architecture.

## How the Queue Works

The `OfflineQueueManager` handles the persistence explicitly with no risk.

1. **Inference Failure & Queueing (`OfflineQueueManager.shared.enqueueCapture`)**
1. **Inference Failure & Queueing (`OfflineQueueManager.shared.enqueueCapture`)**
   When a hiker takes a picture miles away from a cell tower, the UI passes the raw capture and active telemetry context (like `gpsLatitude`, `gpsElevation`, `weatherCondition`, `weatherTemperatureF`, `subjectDistanceInMeters`, and `locationName`) completely decoupled into a single `CaptureTelemetry` object struct mapping to `InferenceEngine.analyze(imageData:telemetry:)`. 
   
   *Note: Legacy environmental variables (such as `cameraPitchDegrees`, `compassHeading`, `uvIndex`, etc.) were completely purged from `CaptureTelemetry` logic to aggressively constrain edge serialization byte limits natively, focusing solely on high-value heuristics.*

   To strictly protect this hardware data from premature UI deallocations across asynchronous boundaries, the telemetry payload is actively instantiated structurally prior to explicitly setting the `imageToCrop` sheet to `nil`. When pushed to the queue, all constraints gracefully drop into the `OfflineQueuedScan` SwiftData model natively. Upon reconnection, the system strictly parses the `subjectDistanceInMeters` offline float back into the stringified `depthScaleText` (e.g. `1.5 meters`) perfectly preserving LiDAR bounds. To entirely prevent application storage block bloat on long off-grid excursions, the engine inherently detaches a compression loop mapping the 12MP buffer down into a highly compressed JPEG natively before any networking attempt. 
   **The Circuit Breaker (`CircuitBreakerManager`)**: To protect the UI from hanging permanently on degraded 3G networks or backend outages, the inference loop routes through `CircuitBreakerManager`. If subsequent HTTP errors or timeouts threshold limits natively, the circuit "trips". Any active tripped state instantly forces all new camera captures dynamically straight to the offline queue immediately, completely bypassing useless network connections to guarantee a 0-latency shutter experience.
   
   If `InferenceEngine` subsequentally encounters a `URLError` or network timeout natively interacting with the backend, it securely catches the failure. **Critically, the Offline Queue is strictly a Merian Pro feature**. Inside the catch block, the engine verifies `RevenueCatManager.shared.isProActive`. If the user is on the free tier, the engine throws `APIError.proRequiredForOfflineTracking`, refunds their scan token organically, and fires a `TriggerPaywall` notification (which `CameraViewModel` catches instantly to display the paywall). If they are Pro, it delegates the newly constrained payload instantly to `OfflineQueueManager.shared.enqueueCapture` (which now correctly binds the encapsulated `CaptureTelemetry` object exactly from the failure point natively). Structurally isolating this step securely, `.enqueueCapture` spawns a `Task.detached(priority: .userInitiated)` explicitly throwing the ~300KB NVMe `Data.write` physical disk commit permanently off the UI `@MainActor` thread. This mathematically avoids Main Thread file I/O locks natively, completely preventing the camera viewfinder from stuttering during capture. Once the physical bytes safely clear hardware cache validations natively into the iOS `URL.documentsDirectory`, an `await MainActor.run` jump back occurs where a new `OfflineQueuedScan` database object is written securely into the physical `.modelContainer()` using Apple `SwiftData`. Furthermore, if the user explicitly pushes the app into the background during a mid-flight active inference, `AppDIContainer.handleBackgroundPhase` explicitly intercepts the native `.background` cycle, rigidly verifying `revenueCatManager.isProActive` once more, and rescues the payload. Critically, to prevent the 12MP uncompressed capture from leaking onto the user's hard drive, the AppDelegate boundary prioritizes extracting the `inferenceEngine.activeCompressedPayload` (the ~300KB downsampled footprint) first, completely protecting device storage. It explicitly reads the active sensory context layers directly off the `InferenceEngine` state to autonomously map a fresh `CaptureTelemetry` object to securely pass exactly into `.enqueueCapture` to ensure background-rescued scans preserve their complete physical dataset (weather, distances, logic) mapping perfectly offline before suspending the app layer.

2. **Network Awakening (`NWPathMonitor`)**
   The `NWPathMonitor` instance listens natively to the internal cellular stack continuously. When a connection flips `.satisfied`, the manager debounces for 1,000 milliseconds to guarantee the pipeline has completely stabilized without thrashing before starting processing.

3. **Background Processing & Payload Compression (`URLSessionConfiguration.background`)**
   The manager checks that the queue isn't already syncing and securely verifies `!HardwareOrchestrator.shared.isExpeditionModeActive` to inherently prevent background cellular battery drain on low-power devices. It then natively engages `SyncStateManager.shared.beginSync(itemCount:)` securely broadcasting the exact integer volume of pending payloads directly to the global `@MainActor` without crashing. This explicitly powers any passive UI indicators (like spinning cloud icons in the Scans library) cleanly mapping state without locking up the upload.
   It then explicitly bounds a `FetchDescriptor` query with an initial `descriptor.fetchLimit = 50` to pull a healthy queue backlog. Critically, the manager safely queries `backgroundSession.allTasks`, explicitly filtering out `PendingScanPayload` items that are currently actively mid-flight mathematically. Only *after* dropping the active background uploads does it safely truncate the array precisely down to `.prefix(5)` to chunk the processing limits safely preventing edge timeout faults. This robust architecture completely structurally neutralizes "Queue Stall" bugs where the first 5 payload items blocking the database fetch inherently match the 5 items mid-flight, ensuring the queue continuously iterates natively. It generates raw Presigned URLs from `/generate-upload-urls`.

   Critically, the core payload loop is securely initialized explicitly within a `Task.detached(priority: .background)` block, completely escaping the singleton's `@MainActor` confinement. Inside the detached task, a precise `@ModelActor BackgroundDatabaseActor` strictly intercepts the `modelContext.fetch` generation natively decoupling the heavy SwiftData persistent fetch entirely from the main thread. It explicitly maps the data natively into purely standard thread-safe Tuple matrices (`PendingScanPayload: Sendable` lightweight structs) eliminating all `EXC_BAD_ACCESS` data race execution crashes and UI freezes. Inside this detached thread, the exact constrained `1:1` `~140KB` geometric payload successfully generated initially by `ImageCropperView` is natively loaded straight from `URL.documentsDirectory` efficiently. It safely skips secondary memory-heavy downsampling operations because the payload logic is inherently resolved at crop time seamlessly! Strict care was taken during crop generation to guarantee thread safety: while memory limits are securely crunched out via raw `cgImage.cropping` inside detached `autoreleasepool` thresholds, the final `UIGraphicsImageRenderer` render pass explicitly forces evaluation back out onto an `await MainActor.run` jump, completely destroying unsafe UIKit background thread crashes globally. State interactions are cleanly routed back via safe `await MainActor.run` enclosures.
   This pre-compressed payload is safely cached into a temporary `URL.cachesDirectory` file explicitly utilizing both the `scanId` and `index` iterator as its unique string to rigidly protect multiple images belonging to the same pending scan from over-writing each other during bulk cellular transmission natively. Prior to physically caching the active payload out to a temporary directory in preparation for Cloudflare R2 bounds, the system aggressively verifies disk boundaries utilizing `.fileExists(atPath:)`. If a user implicitly swept their local library natively during a multi-day off-grid excursion (wiping the reference `.jpg` bytes cleanly before the pipeline synced), building an active URLSession upload task physically over a non-existent file forces an instantaneous failure loop draining battery capacity indefinitely. By expressly checking physical bytes and natively forcing a `.softDeleteQueuedScan` tombstone to jump out, dead `scanId` variants efficiently skip iteration breaking the infinite retry bounds entirely. It is then cleanly handed directly into a strictly isolated native iOS `URLSession` Background Queue natively. The Operating System assumes full responsibility for byte transmission, massively accelerating upload times over spotty 3G cellular boundaries.
   _Note: A strict `UIBackgroundTaskIdentifier` map requests up to 30 seconds of explicit background execution time from iOS. An `expirationHandler` is wrapped locally around the presigned URL generation phase to immediately stop the queue loop, leaving remaining items safely in SwiftData without corrupting data if the OS randomly runs out of time on weak 3G connections. To strictly eliminate duplicated `#if os(iOS)` preprocessor macros and `endBackgroundTask` loops across `enqueueCapture`, `syncPendingScans`, and background URLSessions, the architecture invokes a unified static execution abstraction natively: `BackgroundTaskWrapper.execute(name:operation:)`. This rigid generic boundary exclusively handles registering the active memory environment, securely tracking the OS expiration fallback closures, and perfectly invoking `.endBackgroundTask` inside an implicit `defer` block safely guaranteeing `URLSession` callbacks seamlessly execute over thread boundaries out of the core user grid logic safely._
4. **Upload Lifecycle via URLSession Delegates (iOS Background Hooks)**
   - **Step A:** iOS natively transmits the data into the Staging bucket.
   - **Step B:** Once bytes finish landing securely, SwiftUI invokes the `@UIApplicationDelegateAdaptor` inside `AppDelegate`, transferring the `handleEventsForBackgroundURLSession` handler straight to `OfflineQueueManager`.
   - **Step C:** Native constraints fire `.urlSession(_:task:didCompleteWithError:)` triggering locally. To completely prevent iOS Watchdog application suspensions mid-return, the closure explicitly intercepts execution synchronously immediately mapping into a single comprehensive async `Task { ... }` sequence boundary. Crucially, the manager executes an `await MainActor.run { ... }` jump to strictly grab a `UIBackgroundTaskIdentifier` bounding box sequentially, explicitly forcing the pipeline to yield before evaluating any conditional `guard let` returns natively. This guarantees the background hardware identifier physically locks into iOS memory before any subsequent logic executes, permanently ending fatal orphaned Task leaks! Crucially, to prevent UI sync state desynchronization, `SyncStateManager.shared.completeSync()` is strictly dispatched to the `@MainActor` exclusively when `session.allTasks.isEmpty` natively resolves to true. This eliminates immediate false-positive hidden UI indicators when the user sweeps the queue array while background connections are still natively mid-flight. The queue manager explicitly checks the HTTP status boundary. If a user has been off-grid longer than 24 hours causing their Cloudflare R2 presigned URL to expire (yielding a `403 Forbidden`), or if transient backend service errors occur (`500`, `502`, `503`, `504`), the manager strictly prevents soft-deleting the payload. It cleanly sweeps the `tempFileURL` buffer natively while preserving the `OfflineQueuedScan` safely within SwiftData, guaranteeing a fresh URL generation safely triggers on the next `.satisfied` network cycle. **Wilderness Offline Hydration:** If completely successful (HTTP `200`), the queue manager structurally intercepts the active `CaptureTelemetry`. If the original offline scan lacked `weatherCondition` because it was captured deep off-grid, it natively binds the underlying `CLLocation` coordinates and original `Date` timestamp and triggers absolute synchronous execution of `EnvironmentContextManager.shared.fetchHistoricalContext`. This natively hydrates perfect retroactive weather context precisely mapping to the hour the image was captured! It then cleanly fetches the uploaded file via edge bounds (`MerianNetworkClient.shared.analyzeSubject`), retrieving the fully resolved JSON inference payload securely.
   - **Step D:** The returned taxonomy is passed securely to a dedicated `BackgroundDatabaseActor` where it decodes the payload. Crucially, rather than executing brittle `FileManager.default.moveItem()` hardware hooks that permanently orphan source bytes upon SwiftData insertion crashes or manually rebuilding JSON data mappings independently causing drift, the backend leverages the central `init(fromEdgeResponse: ...)` securely. Passing raw `edgeRes` payload cleanly through a unified object drops boilerplate natively while isolating `SwiftData` bugs. It safely persists the fully identified creature explicitly into the user's `SwiftData` Scans completely inheriting the offline payload seamlessly without sandbox destruction natively. Finally, `processAndCleanupOfflineScan(scanId:)` automatically cleans up the old explicit queue references purely on its own background thread. Crucially, to obliterate massive gigabyte-scale Disk Space Storage Leaks structurally, it iteratively calls `FileManager.default.removeItem` entirely flushing the underlying physical `.jpg` payloads off the user's SSD natively before resolving the cleanup transaction completely insulating the 60FPS UI from stuttering during batch updates!

## Deletions in Offline Environments

Scan permanence and user privacy dictates that explicitly deleted datasets are permanently erased. Merian ensures these erasures map natively even fully offline:

1. **Optimistic Destruction (`ScanRepository.eradicateScan`)**
   When a user destroys a scan natively via the `.contextMenu`, the underlying filesystem `FileManager` aggressively detaches the image cache bytes off the physical disk payload immediately mapping a strict 0-byte destruction locally. 
2. **Cloud Deletion Tasking (`PendingCloudDeletionTask`)**
   To bypass connectivity dropping randomly during erasures, SwiftData natively persists a schema-scaled `@Model final class PendingCloudDeletionTask` which queues up the raw `scanId` locally safely bounding the target. The UI aggressively executes `.delete` physically dropping the UX bounding out of sight instantly for the user, executing a fully synchronous optimistic UI drop.
3. **Deletion Interception (`softDeleteQueuedScan`)**
   If the scan being destroyed is actively queueing for its native initial upload stream inside the offline boundary natively, `OfflineQueueManager.shared.softDeleteQueuedScan` explicitly intercepts the background worker terminating the native upload path bounding permanently isolating upload destruction cleanly.
4. **Network Polling Sync (`syncPendingDeletions`)**
   As `NWPathMonitor` wakes the application on cellular bounds mapping, it explicitly runs `syncPendingDeletions()` triggering backend Cloudflare R2 object erasures alongside PostgreSQL tuple obliterations securely executing via a mapped JWT `MerianNetworkClient.shared.deleteScan()` edge payload. The SwiftData queue cleans up the tasks natively inside the `.background` task context mapping.

## Restoring Historical Workloads (Rehydration)

To securely support multi-device logic and app reinstalls, Merian implements a physical boundary bypassing standard fetch drops: 

1. **The Rehydration Native Sync (`syncHistoricalScansDown`)**
   Explicitly embedded in `ScanRepository.shared`, this robust task queries the Supabase Edge natively pulling a joined `scans` + `species_dictionary` PostgreSQL JSON response. It sequentially filters all UUIDs securely querying the edge payloads perfectly bounded inside a `.eq("user_id", value: userId)` isolation. To explicitly protect the UI from stuttering during extreme historical payloads (e.g. 5000+ records), the JSON parsing and raw SQLite `.insert(record)` execution is completely decoupled from the Main Thread, natively routing array ingestion directly into a generic `@ModelActor HistoricalDatabaseActor` processing offline safely.
2. **Lifecycle Execution Hook**
   This synchronization executes aggressively the pure second a user completely transitions from `Ghost` -> `Authed` inside the `SupabaseManager.setupAuthStateListener` or uniquely whenever the app recovers foreground capabilities out of deep bounds (`AppDIContainer.handleActivePhase`).
3. **Ghost-Rendering Image Optimization**
   To entirely avoid destroying device cellular data limits downloading massive multi-gigabyte historical image files physically into iOS, the hydration engine smartly routes the historical Cloudflare R2 URL dynamically sourced directly out of PostgreSQL into the `.localImagePath` and `.additionalImagePaths` properties when the physical photo is absent locally. This completely avoids polluting `.referenceImageUrl` (which strictly maps to explicit Wikimedia or GBIF remote image dictionaries), inherently terminating "duplicate photo" bugs within the Image Carousel. By strictly separating the user's explicit capture URL from general biological references, iOS clients appropriately treat the remote R2 HTTP payload exactly like a `URL.documents` file. The `LocalImageLoader` actor physically evaluates HTTP boundaries implicitly, seamlessly routing physical async caches gracefully so the user downloads exactly what the UI needs exactly when they view it without blocking UI loops or fetching redundant taxonomy image data twice.
