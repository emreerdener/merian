# Supabase Edge and PostgreSQL Engine

Merian employs Supabase implicitly, relying completely on `.xcconfig` obfuscation and Server-Side execution safely decoupled from the physical device.

## Core Schema Structure

The `00001_initial_schema.sql` database file defines the backend architecture. A secondary `00002_user_auth_trigger.sql` schema handles the relationship natively. However, strictly all anonymous users are now identified exclusively by a persistent Keychain-backed `UIDevice.current.identifierForVendor` IDFV.

- **`species_dictionary`**: Tracks every scientifically discovered taxon uniquely mapping directly to native biological descriptors.
- **`scans`**: Logs physical GPS bounds, LLM generated `ai_confidence_score` matrices, UUID bindings, and the corresponding `ecology_type_enum` permanently to the users' streaks.
- **`users`**: Binds the IDFV (or future authenticated UUID) to strict product schemas natively tracking usage limits.
- **`unified_species_count_sync` (`20260320132111_unified_species_count_trigger.sql`)**: Handles the asynchronous data aggregation of unique biological models actively bound to a distinct user. The previously split trigger files have been dropped. It calculates the `total_species_discovered` count in the `users` table via an invisible, unified, and idempotent Postgres `AFTER INSERT OR UPDATE OR DELETE ON public.scans` trigger mathematically mapping the exact `SELECT COUNT(DISTINCT species_id)` physically. This elegantly eliminates Time-Of-Check to Time-Of-Use (TOCTOU) schema drift and sync errors globally on moderation updates.

## The Edge Inference Node (`identify`)

The Deno `/identify` edge function acts as the universal proxy masking logic entirely:

1. **Edge Identity Resolver**: Receives a structured payload containing environmental constraints natively from iOS. Concurrently, it extracts the `user.id` cryptographically from the `Authorization` header natively executing via a centralized explicit `requireAuth(_shared/auth.ts)` middleware abstraction bound globally across all endpoints. Because Merian utilizes modern Asymmetric ES256 Ghost sessions (`signInAnonymously`), the default Supabase `verify_jwt` Edge Middleware is explicitly disabled inside `supabase/config.toml` (`verify_jwt = false`). The Deno handlers explicitly run manual SSR Auth extraction natively via the Supabase SDK, stopping 401 Rejections from Edge Gateway boundaries and eradicating IDOR vulnerabilities.
2. **Direct Base64 Transfer**: Bypasses traditional Cloudflare S3 bridging entirely by natively evaluating the encoded `imageBase64` directly within the memory limit of the Edge function. It instantly extracts the geometry natively converting it into a `Uint8Array`. It passes this `inlineData` explicitly into the `.generateContent` Google context boundary. This purely eliminates network execution dependencies entirely dropping S3 staging evaluations dropping pipeline latency by 1 second instantly!
3. **Legacy AWS Fallback (`_shared/aws.ts`)**: If the iOS subsystem explicitly suppresses the `imageBase64` payload (such as during native OS Background `URLSession` `OfflineQueueManager` operations where the image was physically uploaded natively to S3 rather than passed live), the script drops back elegantly mapping a global instantiation via `getS3Client()`. It safely pulls the R2 Stream evaluating explicit bounding dimensions (`r2Response.headers.get("Content-Length")`) throwing HTTP 413 for any artifacts exceeding 5MB, preventing massive payload attacks before pushing an `ArrayBuffer` evaluation!
4. **Google Gemini Pro/Flash Extraction**: Prompts `gemini-2.5-flash` or `gemini-2.5-pro` dynamically based on the verified `subscription_tier` fetched securely natively from the Ghost User. It forcefully binds generation mathematically with `temperature: 0.1` forcing rigid biological JSON schema outputs natively.
5. **Asynchronous Edge Decoupling**: Dynamically binds `EdgeRuntime.waitUntil` parsing the taxonomy payload natively directly to the iOS client, decoupling intense database mutations dynamically (Moderation Pipeline, GBIF Data Scrape, Wikipedia Integration, and PostgreSQL UPSERTs) explicitly into background edge workers completely to prevent network UI bottlenecks.
6. **Moderation Pipeline (`moderation.ts`)**: Evaluates the explicit Gemini Safety Ratings before any logic fires. Unsafe media throws an exception bounding the user with a `SHADOWBANNED` token intuitively incrementing abuse strikes natively and immediately wiping the R2 media natively. Safe media falls through to the **Rolling Cloud Window** storage policy mapping explicit `.userTier` strings seamlessly triggering Cloudflare R2 Lifecycle policies natively. To optimize V8 Garbage Collector memory bottlenecks, iterating over `Uint8Array.from(atob(...))` was eradicated and completely replaced by Deno's native Rust-backed `decodeBase64` standard library implementation.
7. Decodes the taxonomy payload passively. Extracts enriched biological context via Wikipedia (including deep-linked URLs and physical paragraph `extract` payloads) and GBIF APIs natively wrapped behind secure `AbortSignal.timeout(2500)` locks. Critically, these enrichments are executed entirely concurrently via `Promise.allSettled()`, cleanly halving edge execution time and gracefully returning metadata immediately back down to iOS. Additionally, Gemini responses are rigorously run through a strict multiline regex structure (`match(/\{[\s\S]*\}/)`) to explicitly extract only the JSON payload, neutralizing all conversational hallucinations and Markdown blocks natively before `JSON.parse` is hit to prevent HTTP 500 crash-loops.
8. **Swift `JSONDecoder` Null Protection**: When the iOS client queries Supabase/PostgREST natively and expands the `species_dictionary(*)` relationship, fields like `descriptions` are assembled as JSONB objects in PostgreSQL. Because `wikipedia_extract` could be safely `null` during edge execution (e.g., if Wikipedia lacks a page), the resulting JSON contains `{"wikipedia": null}`. The iOS decoding struct *must* strictly type this as `let descriptions: [String: String?]?` (Optionally wrapped Optional Strings). Declaring it safely as `[String: String]?` will immediately and silently crash the standard Swift `JSONDecoder` with a `typeMismatch`. Additionally, because Swift Dictionaries are internally unordered, iOS clients must strictly parse exact keys (e.g. `descriptions?["wikipedia"]`) rather than iterating values via `.compactMap { $0.value }.first`, which will randomly scramble Insight texts and Wikipedia extracts natively, corrupting the UI data bindings during offline historical sync operations.
9. Physically executes a strictly secured `supabaseAdmin.from('species_dictionary').upsert()` action injecting `{ onConflict: "scientific_name", ignoreDuplicates: true }` chained with a `.select().maybeSingle()`. This explicit atomic transaction completely neutralizes Time-Of-Check to Time-Of-Use (TOCTOU) race condition bugs causing fatal PostgreSQL `UNIQUE` violations when two identical live captures happen concurrently. By specifically using `.maybeSingle()`, the system elegantly escapes the fatal `PGRST116` runtime error when Postgres intrinsically returns 0 rows during a duplicated upsert block. The edge function gracefully catches this null resolution and seamlessly runs a secondary `.select().single()` to fetch the existing biological UUID natively ensuring `scans` table mappings are never dropped logically. Both operations dynamically leverage the backend `SUPABASE_SERVICE_ROLE_KEY` to securely bypass global Row Level Security limits blocking users from vandalizing the dictionary.
10. Checks if the GoTrue JWT `user.id` natively exists _before_ running moderation validations. If it doesn't, physically `.upsert()`s a Ghost User cleanly preventing SQL Foreign Key crash boundaries natively inside the Deno node gracefully and allowing abuse strikes to track first-time offenders correctly. Critically, this upsert rigidly injects `{ onConflict: "id", ignoreDuplicates: true }` to ensure any existing users who have upgraded to a RevenueCat Pro tier are completely ignored and their `subscription_tier` is not silently wiped back down to `"free"`.
11. Drops smoothly down securely bypassing standard RLS mapping through `supabaseAdmin.from('scans').insert()` to natively bind the scan transaction precisely to the authenticated `user.id` from the JWT securely overriding the hardware's payload. During this exact invocation, the Edge Function permanently commits all extracted environmental telemetry (Pitch, Heading, Humidity, Time of Day, Month, Locale, Semantic Location, Flash State, Lidar Depth Scale) alongside **Google Cloud LLM Token Metrics** (`llm_prompt_tokens`, `llm_candidate_tokens`, `llm_total_tokens` cleanly extracted from Gemini's `usageMetadata`) directly into their respective PostgreSQL columns locking the unified payload tracking.
12. Safely passes the `.json` payload formatted exclusively as a nested `{ success: true, data: { ... } }` array back into the waiting Swift boundary over network lines securely bypassing double-encoded JSON crashes natively.
13. **V8 Execution Abstractions & Connection Pooling**: 
    - Across all Edge Functions (`identify`, `generate-upload-urls`, `safe-delete`, `block-user`, `export-dwca`, `get-filtered-discovery-feed`, etc.), redundant `requireAuth` boundaries and CORS `OPTIONS` preflights are strictly eliminated securely bypassing standard boilerplate via `_shared/edgeHandler.ts` wrapping endpoints dynamically inside `withEdgeHandler()`. Additionally, physical return blocks (such as `new Response(JSON.stringify({...}), {headers: corsHeaders, status: 200})`) have been systematically replaced natively with a unified `jsonResponse(payload, status)` helper, extensively reducing V8 isolate memory footprints and instantiation overheads cleanly across the network bounds.
    - Repetitive S3 and R2 string formatting instantiations explicitly mapping `Deno.env.get` loops are abstracted natively down into `getR2Config()` securely instantiated within `_shared/aws.ts`.
    - GoTrue JSON Web Token validations structurally bypass localized per-request `createClient` initializations, elegantly executing `await supabaseAdmin.auth.getUser(...)` implicitly behind the `edgeHandler` middleware.

## The Webhook Node (`revenuecat-webhook`)
Merian natively ties into RevenueCat's Webhook architecture to drive async tier boundaries (`pro` or `free` migrations). Because Deno Enforces strict 10-second processing limits, the webhook node explicitly delegates heavy S3 operations (like bulk moving payloads from `/free/` into `/pro/` buckets) off-the-edge by natively deferring them via `EdgeRuntime.waitUntil(promise)`. This routine strictly queries the user's orphaned `public_uploads/free/` objects from the PostgreSQL `scans` table and executes explicit `AWS SDK PUT` copy commands bridging the payload securely into the permanent `/pro/` tier. Crucially, to prevent IDOR vulnerabilities (Insecure Direct Object Reference) resulting in malicious S3 delete executions, the script strictly validates that the `originalUserId` parsed directly out of the `image_storage_urls` path matches perfectly with the authenticated `userId` associated with the webhook trigger completely securing data boundaries. 
Furthermore, it actively protects against "Hoarding Exploits" via the explicit RevenueCat `EXPIRATION` trigger. If a user downgrades their account, the webhook violently reverses the flow executing an identical decoupled AWS structural sweep dynamically migrating all `.jpg` objects securely backwards from `/pro/` to `/free/` directly returning the account to the strict 90-day Cloudflare R2 purge cycle logic.
Finally, the webhook strips dangling AWS signature query string parameters dynamically directly out of the `URL` before saving it to PostgreSQL, entirely bypassing Cloudflare R2 `403 Forbidden` authorization lockouts on mathematically changed object keys natively. This guarantees an instant `HTTP 200` back to RevenueCat without triggering 504 Retry timeouts. **Additionally, AWS S3 mapping initialization paths explicitly consume the centralized `const { s3Client, bucketName, endpoint } = getR2Config()` abstraction exported from `_shared/aws.ts`, explicitly eradicating duplicate raw string mapping loops natively.**

## The Scientific Export Node (`export-dwca`)
Researchers deploying global data extractions trigger the `export-dwca` edge function. This node strictly bounds memory and ensures PII privacy natively:
1. **OOM Streaming Boundaries**: Defeats massive 256MB Deno heap crashes on monolithic `JSZip.generateAsync()` generations. Leverages a native `ReadableStream` combined with AWS `UNSIGNED-PAYLOAD` header signatures cleanly funneling binary streams directly up to R2 chunk bytes progressively instead of locking memory structures in the V8 heap. 
2. **Cryptographic Geoprivacy Limits**: Global exports automatically sever the literal `user.id` link to prevent stalking. All distributions are algorithmically pseudo-anonymized executing native `crypto.subtle.digest` via `SHA-256`, structurally generating stable string bounds (e.g. `merian_user_a785f2b...`) so scientists can verify isolated user streaks accurately without ever decrypting their physical Supabase token hashes globally.
34. **Event Loop Starvation Protocols**: Generating Darwin Core Archives natively extracts tens of thousands of occurrences securely masking global user identities via `crypto.subtle.digest` logic. Executing this massive cryptographic pipeline fully concurrently via a monolithic `Promise.all` completely starves the Deno V8 Javascript event loop, blocking HTTP threads and producing lethal `504 Gateway Timeout` errors. Merian rigidly forces the mapped cryptographic digests into properly sequential sub-batches (chunks of 50) using a `for...of` loop. This physically yields the CPU resources back into the event loop dynamically across sub-array iterations, preserving deep backend thread latency and stopping memory spike exhaustion dead in its tracks.
## The Edge Moderation Node (`block-user`)

To completely bypass complex Row Level Security (RLS) policies acting upon anonymous Device ID (IDFV) boundaries implicitly, Merian completely routes Toxicity blocking protocols through this specific serverless Deno node:

1. Native iOS extracts the internal UUID inside `DeviceIdentityManager.shared.deviceId` explicitly and securely queries its active JWT. If the session is missing, it intelligently throws a `NetworkError` preventing accidental API identity overwrites during off-grid operations.
2. The payload `{"blocked_id": "..."}` is pushed securely via `.xcconfig` REST headers natively. The boundary extracts the `blocker_id` exclusively via a native `supabase.auth.getUser()` verification correctly integrating with GoTrue's ES256 key architecture natively.
3. The instance bypasses PostgreSQL Row Level locks by executing `supabaseAdmin.from('user_blocks').insert()` natively using the backend `SUPABASE_SERVICE_ROLE_KEY`, instantly locking off the social data boundary natively without exposing the DB array tables back to the iOS end-user grid.

## Security & Environment Validation

To permanently eradicate reverse-engineering threat vectors, iOS is explicitly isolated from executing LLM capabilities directly:

- The `GEMINI_API_KEY` is entirely scrubbed and explicitly **removed** natively from the iOS client bundle (`Info.plist` & `.xcconfig`).
- Instead, the `MerianApp` binary relies 100% on the `identify` and `block-user` Supabase Edge Functions which safely encrypt the LLM API Keys explicitly within the Supabase Cloud backend.

## Database Indexing & Performance

To gracefully handle future database scale natively and prevent Postgres sequential table scans causing timeout errors on the Edge function:

- **`00003_performance_indexes.sql`** generates `CREATE INDEX CONCURRENTLY` hooks bounding the following high-frequency identifiers:
  - `idx_species_dict_scientific_name` on `species_dictionary (scientific_name)` for immediate species classification lookups natively halving Edge processing limits.
  - `idx_scans_user_id` on `scans (user_id)` to gracefully render users' streaks without sequential database scans.
  - `idx_scans_discovery_feed` on `scans (geoprivacy, is_live_capture, timestamp DESC)` to index massive global map discovery fetches smoothly.
  - `idx_scans_user_species` on `scans (user_id, species_id)` to gracefully eliminate sequential index latency when evaluating Postgres Triggers aggregating total distinct species metrics `COUNT(DISTINCT species_id)`.
  - `idx_scans_lifecycle` on `scans (timestamp) WHERE image_storage_urls != '{}'` to explicitly protect the daily storage cleanup cron job from performing sequential table scans across the entire global database just to find 90-day-old Free tier captures.

## Storage Economics & Lifecycle Syndication

To prevent R2 bucket expirations from natively corrupting Postgres payloads through dead links, the database continuously governs consistency:

- **`00004_storage_lifecycle_sync.sql`**: Configures a `pg_cron` server-side background job that safely purges strictly the `image_storage_urls` array on any `.subscription_tier = 'free'` entries older than 90 days. This runs securely once per day at 02:00 UTC without manually wiping the underlying row, intentionally preserving globally distributed biological context and GPS matrices.
- **`get-filtered-discovery-feed`**: Contains a strict filter constraint `.not("image_storage_urls", "eq", "{}")`. This explicitly protects the global iOS public feeds from accidentally crashing or rendering ugly blank components by shielding expired payloads natively out of the Discovery pipeline altogether. Furthermore, it securely evaluates a `users!inner(is_shadowbanned)` join coupled with an `.eq("users.is_shadowbanned", false)` bounding map. This explicitly resolves the `SUPABASE_SERVICE_ROLE_KEY` bypass of Row Level Security (RLS), actively enforcing global Shadowban isolation dropping toxic media directly off the ecosystem instantly without requiring iOS patches.
- **Graceful Degradation Hook**: Scans whose visual media payloads have permanently rolled out of the R2 tier fall back into a Stage 3 Metadata-Only state internally on `ScansThumbnailView` and `AsyncLocalImageView`. Rather than thrashing empty `ProgressView()` loops, the API replaces the image natively with an interactive `.ultraThinMaterial` glass pane rendering the `archivebox.fill` icon. This completely eradicates 404 caching errors and embraces Radical Transparency formatting for legacy offline discovery histories safely!

### Cloudflare R2 Object Lifecycle Rules

To physically align the buckets with our Postgres `pg_cron` jobs and safely delete payload data natively to prevent storage bloat, Merian requires the following 4 Object Lifecycle Rules configured natively in the Cloudflare R2 Dashboard under **Settings -> Object Lifecycle**:

1. **Default Multipart Abort Rule**
   - **Prefix:** `--`
   - **Action:** Abort incomplete multipart uploads after `7` days
2. **Free Tier Expiration**
   - **Prefix:** `public_uploads/free/`
   - **Action:** Delete objects after `90` days
3. **Purge staging objects after 1 day**
   - **Prefix:** `staging/`
   - **Action:** Delete objects after `1` day
4. **Quarantine Cleanup**
   - **Prefix:** `quarantine/`
   - **Action:** Delete objects after `1` day

## Customer Support & ML Feedback Loop (`flag-issue`)

To continuously refine the AI models and build a high-quality human-verified dataset ("Golden Dataset"), Merian allows users to flag incorrect taxonomy outputs directly from the `InsightSheetView`:
- **`flag-issue`**: This serverless Edge Node securely ingests authenticated POST requests containing `scanId`, `flagReason`, and `userSuggestion`.
- **`flagged_reviews` SQL Table**: `00005_flagged_reviews.sql` generates this schema, seamlessly mapping flagged scans natively back to the reporting `user_id`. It defaults to a `PENDING_REVIEW` state.
- **`scans` Table Cascade**: The Edge Function immediately updates the parent scan's boolean `is_flagged` state to `true` and appends debugging data straight into the `human_intervention_notes` column to isolate the failure logic.

## Account Deletion & Data Preservation (`safe-delete`)

To balance user privacy (GDPR/CCPA compliance) with scientific data fidelity, account deletions utilize a specialized edge node triggering a backend RPC: **`00006_apply_user_tombstone.sql` / `apply_user_tombstone.sql`**. This generates a `public.apply_user_tombstone(target_user_id UUID)` PL/pgSQL function. Instead of cascading deletions that would wipe thousands of biological insights off the global map, it reassigns the user's `scans` to a permanent anonymous `00000000-0000-0000-0000-000000000000` tombstone user and flags them as `is_tombstoned = true`. Afterward, it safely cascades and destroys the original user schema and telemetry without breaking the structural biological maps. Note: The anonymous tombstone user instantiation correctly targets the existing `current_streak_count` schema rather than legacy strings.

Upon returning a `200 OK`, the iOS client natively completes the loop by executing `supabase.signOut()` and tearing down the SQLite database strictly routing through `ScanRepository.shared.purgeAllData()`, obliterating all orphaned imagery caches on device.

## Scan Erasure & The Deletion Pipeline (`delete-scan`)

To empower absolute privacy controls natively, users can obliterate physical scans globally. This pipeline completely severs the data from Supabase and Cloudflare R2 simultaneously:
1. **Authentication Override**: The Edge Function explicitly disables GoTrue Middleware globally (`verify_jwt = false` in `config.toml`) and manually extracts and cryptographically verifies the header JWT. This fully protects against IDOR boundary leaks locking deletion exclusively to the `scan.owner_id`.
- Ensure proper RLS configuration bounds on all remote data streams statically isolating reads.

#### V8 Execution Abstractions
- **Explicit Deno ES Modules**: To forcefully prevent Supabase CLI cloud bundling failures during runtime executions (which frequently ignore local `deno.json` import maps throwing "relative import path not prefixed" failures), Merian strictly enforces **Direct HTTP Module Injections**. All edge dependencies natively declare explicit raw string bounds (e.g., `https://esm.sh/@supabase/supabase-js@2.45.0` or `https://deno.land/std@0.224.0/http/server.ts`). To prevent the IDE from thrashing `deno-lint(no-import-prefix)` errors locally across these pure network strings recursively, the primary `supabase/functions/deno.json` explicitly bounds an `"exclude": ["no-import-prefix"]` override shielding the codebase.
- **`_shared` Utilities**: Foundational CORS headers and Supabase authentication validators (`requireAuth`) securely operate within the `supabase/functions/_shared/` domain boundary mapping logic natively preventing duplicate `req.headers.get("Authorization")` boilerplate across 10 discrete execution files reliably!
2. **R2 Integration**: The Deno edge node extracts the `image_storage_urls` array natively from Supabase securely via `.select()`. Since `image_storage_urls` exclusively stores public Cloudflare Web bindings (`https://media.merian.app/...`) to allow `URLSession` inside iOS to load imagery without triggering `HTTP 400 Bad Request` S3 authentication blocks, the edge node mathematically replaces the target strings back into safe `https://<account>.r2.cloudflarestorage.com/<bucket>/...` domains at runtime. It then explicitly passes these reconstructed S3 paths into `AwsClient` (via `aws4fetch`), successfully issuing `DELETE` payloads seamlessly scrubbing the bytes natively off Cloudflare without stranding payloads.
3. **Database Erasure**: A simple `.delete()` sweeps the `scans` tuple entirely off the maps.
4. **Gamification Integrity Trigger**: A strict Postgres PL/pgSQL function (`decrement_user_species_count()`) fires automatically upon `AFTER DELETE ON public.scans`. If the user just explicitly deleted their absolute *last* known physical scan of a specific `species_id`, the function automatically drops their unified `users.total_species_discovered` index seamlessly by `1` without negative integers natively maintaining streak telemetry perfectly regardless of offline delays seamlessly!
